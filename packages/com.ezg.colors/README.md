# com.ezg.colors

HTML/X11 color enum, hex↔Color32 conversion, and Unity rich-text color tagging helpers.

## Source mapping

`Assets/_Game/4.CORE/Modules/Colors` → `packages/com.ezg.colors/Runtime/`

## Assembly

`Ezg.Colors` (auto-referenced)

## Dependencies

None — depends only on `UnityEngine` (engine auto-reference).

## Peer requirements

None.

## Install

Add to `Packages/manifest.json`:

```json
"scopedRegistries": [{
  "name": "Easygoing code base",
  "url": "https://upm-registry-worker.developer-a1f.workers.dev",
  "scopes": ["com.ezg"]
}],
"dependencies": {
  "com.ezg.colors": "0.1.0"
}
```
