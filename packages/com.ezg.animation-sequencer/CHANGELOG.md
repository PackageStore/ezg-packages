## [0.5.6] - 2026-06-14

- Add missing .meta files for README.md, CHANGELOG.md, package.json to fix Unity "immutable folder" import errors


# Changelog

## [0.5.5] - 2026-06-05

- Initial publish under the `com.ezg.animation-sequencer` id. Vendored verbatim
  from `brunomikoski/Animation-Sequencer` v0.5.5 (source folder
  `Assets/_Project/3rdParty/Animation-Sequencer`) and rescoped to the `com.ezg`
  registry. Assembly names, source, LICENSE, and original README are unchanged;
  only the `package.json` `name` (→ `com.ezg.animation-sequencer`) and `unity`
  (→ `6000.2`) fields were adjusted.