# Changelog

## [0.1.0] - 2026-06-20
### Added
- Initial release extracted from `Assets/_Project/3rdParty/UIParticleSystem`.
- Runtime assembly `Ezg.UIParticleSystem` with `UIParticleCanvas`,
  `UIParticleDepthObject`, `UIParticleDepthObjectInfo`, `UIParticleEnums`, and
  `UIParticleRectTransSizeFollower`.
- Shaders: `UIParticle`, `UIParticleWithDistortion`, and the `UIParticleMask`
  depth/cull render pair, plus the `UIDepthLib` include.
- Demo scene under `Samples~/Example/` and vendor documentation under `Documentation~/`.
