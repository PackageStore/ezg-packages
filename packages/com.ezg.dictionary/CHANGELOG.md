# Changelog

## [0.1.2] - 2026-06-08

Fix asmdef .meta importer type: replace `AssemblyDefinitionImporter` with `DefaultImporter` so Unity 6 correctly imports the assembly definitions and compiles the .cs files.

## [0.1.1] - 2026-06-08

Fix missing `package.json.meta`, `README.md.meta`, `CHANGELOG.md.meta` â€” Unity requires these to import the package correctly.

## [0.1.0] - 2026-06-08

Initial publish â€” `SerializableDictionary<K,V>` with Unity serialization support and Editor property drawers.

